dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url "https://muxinc.jfrog.io/artifactory/default-maven-release-local" }
    }
}
rootProject.name = "Mock MuxStatsExoPlayer"
include ':app'
